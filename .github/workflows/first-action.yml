name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: | 
          echo "hello, world"
          echo "wassup, bmore!"
      - name: Print Goodbye
        run: echo "have a good one, bmore!"
